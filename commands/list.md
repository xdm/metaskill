---
description: Show skills installed by metaskill (from its lock file)
allowed-tools: Bash(node:*), Bash(metaskill:*)
---

!`{{METASKILL}} list`

Show the table above to the user as-is, in a code block. If it says nothing
was installed yet, say that in one line instead. Add nothing else.
